# 2026-04-26 - Sunday

## M5Timer Recovery ✅
- **09:00**: User flashed firmware at wrong offset (0x0000 instead of 0x10000) — bricked device
- **Recovered** by flashing correct old version
- **Root cause**: Commit `6b83db9` changed buzzer to `ledcAttach()` (ESP32 Arduino 3.x API)
- **User's build environment uses ESP32 Arduino 2.x API**: `ledcSetup()` + `ledcAttachPin()`
- **Fix**: Reverted buzzer.cpp to 2.x compatible code — tone fixed!

## Tools-Suite Bug Fix Marathon

### Bugcheck Report (Completed 2026-04-26 ~12:30)
- **18 issues found** across tools-suite: 1 Critical, 2 High, 8 Medium, 7 Low
- Files analyzed: 49 | Health: Fair
- Key issues:
  - CRITICAL: `shared/utils.js` not cached by any SW (offline breaks)
  - CRITICAL: `escapeHtml()` doesn't escape quotes (XSS vector)
  - CRITICAL: Duplicate SW registration in life-pattern-generator
  - HIGH: `resetButtons()` undefined in sync pages
  - HIGH: ReDoS in CSV number regex
  - HIGH: Clipboard API no error handling

### Fix Round 1 (Subagent)
- **Session**: `agent:main:subagent:fb385ba6-a62d-40e0-8eae-e0ed93f2354d`
- **Runtime**: 11m5s
- **Claims**: Fixed 23+ issues, 3 commits (`454a399`, `e4c365d`, `8010a13`)

### Verification (Subagent)
- **Session**: `agent:main:subagent:98e142e9-2e99-48f3-a46a-abeb9139a257`
- **Runtime**: 2m17s
- **Found 3 NEW bugs introduced by fixes**:
  1. 🔴 **CRITICAL**: `diff-viewer/app.js` syntax error (dangling duplicate code block) — app won't load
  2. 🟡 `diff-viewer/sw.js` missing `'../shared/utils.js'` in cache
  3. 🟡 `jwt-decoder/index.html` double-escaping (`&` → `&amp;amp;`)

### Fix Round 2 (Manual)
- Fixed all 3 verification issues
- **Commit**: `a802b21` — diff-viewer/app.js, diff-viewer/sw.js, jwt-decoder/index.html
- All issues now resolved

## Health Check
- Workspace cleaned up (3 files moved, 4 lines removed)
- Node version: v25.7.0
