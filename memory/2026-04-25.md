# Memory — 2026-04-25

## Full Bug Check Completed
- **All 14 projects audited** using 14 parallel subagents (5min timeout each, 6 retried with 10min)
- **201+ total issues found** across all projects
- Report saved: `bugcheck-report-2026-04-25.md`
- Top criticals:
  - QuickNotes: build failure (duplicate anonymous class block)
  - tools-suite: `shared/utils.js` not cached by SW = all tools offline
  - collaboard: CSP weakened + no SRI on PeerJS
  - droptransfer: silent unencrypted fallback + broken heartbeat
  - battery-panic: drain math makes game unplayable (3.6%/sec)
  - atoms3-rainbow: button debounce completely broken

## M5Timer Fixes ✅
- **Session counter resets on manual mode change** (user request)
- Fixed switch-mode timeout fraud (no longer counts fake sessions)
- Fixed stale `pongWaitStartTime` bug on SYNC re-entry
- Fixed double NVS flash write on timer expiry
- Fixed `lastSavedMinuteBoundary` reset
- Removed inline `extern` declarations in sync.cpp
- Fixed button triple-click timing
- Refactored timer logic (single `_getNextMode` helper)
- Improved `fromString()` validation
- **Committed**: `7f93f6c`

## QuickNotes Fixes (Partial)
- Attempted fixes for build failure + multiple bugs
- **User said STOP at 19:27** — all work halted, reverted changes
- Desktop has `QuickNotes-backup-20260425-135028` (original) and `QuickNotes-fixed` (my edits)
- Need to retry: remove duplicate class block, fix delete, atomic writes, etc.

## SSH Into Windows ✅ Configured
- Key-based auth working: `ssh Lenovo@127.0.0.1`
- PTY sessions work (tested with `whoami` + interactive)
- `authorized_keys` in `C:\Users\Lenovo\.ssh\`
- For admin users, Windows SSH also checks `C:\ProgramData\ssh\administrators_authorized_keys`
- Added SSH section to TOOLS.md

## Workspace Changes
- Created `bugcheck-report-2026-04-25.md` (20KB full report)
- Created `memory/project-roadmap-2026-04-25.md` (fix roadmap)
- Archived `droptransfer-cli` (removed from active projects)
- OpenClaw backup exists: `openclaw-backup.tar.gz` (270MB)

## User Communication
- User prefers action over questions
- User said "make M5Timer the best" — prioritize M5Timer improvements
- User has `sudo` configured in Windows PowerShell
- User's SSH key: `~/.ssh/id_ed25519.pub` copied to Windows

## Current State (End of Session)
- **M5Timer**: Fixed and committed ✅
- **QuickNotes**: Fixes reverted, needs retry ⏳
- **SSH**: Working ✅
- **Next planned**: tools-suite (critical offline bug), then lifelab upgrade
- **All other work**: STOPPED pending user direction
