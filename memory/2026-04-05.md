# 2026-04-05 - Sunday

## Skylight Mod - Bug Fix & Testing ☄️

### Current Status
🧪 **In Testing** — Fixed crash, awaiting verification

### Bug Fix: PlayerJoinMixin (1.21.1 Compatibility)
**Issue**: Crash on world load due to mixin injection failure
**Root Cause**: `PlayerManager.onPlayerConnect()` signature changed in 1.21.1 — added `ConnectedClientData` parameter
**Error**: `InvalidInjectionException: Expected (L...class_8792;L...CallbackInfo;)V but found (...)V`
**Fix**: Updated `PlayerJoinMixin.java`:
- Added import: `net.minecraft.server.network.ConnectedClientData`
- Changed method signature from 3 params to 4: `(connection, player, ci)` → `(connection, player, clientData, ci)`

### Build
```
projects/skylight-mod/build/libs/skylight-1.0.0.jar (31KB)
→ Copied to MC mods folder
```

### Pending
- [ ] Test in-game to verify crash is resolved
- [ ] Verify all photography tools work (K, F, P, C keys)
- [ ] Confirm 1.21.1 compatibility
